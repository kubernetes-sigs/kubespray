---
- name: Verify limited operations reject excluded uncached hosts
  hosts: k8s_cluster
  gather_facts: false
  vars:
    ignore_assert_errors: false
  tasks:
    - name: Run preinstall verification
      ansible.builtin.import_tasks: ../../../roles/kubernetes/preinstall/tasks/0040-verify-settings.yml
