---
- name: Check ansible version
  import_playbook: ansible_version.yml

- name: Ensure compatibility with old groups
  import_playbook: legacy_groups.yml

- hosts: "{{ node | default('etcd:k8s_cluster:calico_rr') }}"
  gather_facts: no
  tasks:
    - name: Confirm Execution
      pause:
        prompt: "Are you sure you want to delete nodes state? Type 'yes' to delete nodes."
      register: pause_result
      run_once: True
      when:
        - not (skip_confirmation | default(false) | bool)

    - name: Fail if user does not confirm deletion
      fail:
        msg: "Delete nodes confirmation failed"
      when: pause_result.user_input | default('yes') != 'yes'

- hosts: kube_control_plane[0]
  gather_facts: no
  environment: "{{ proxy_disable_env }}"
  roles:
    - { role: kubernetes.kubespray.kubespray_defaults }
    - { role: kubernetes.kubespray.remove_node.post_remove, tags: post_remove }

- hosts: "{{ node | default('kube_node') }}"
  gather_facts: no
  environment: "{{ proxy_disable_env }}"
  roles:
    - { role: kubernetes.kubespray.kubespray_defaults, when: reset_nodes|default(True)|bool }
    - { role: kubernetes.kubespray.remove_node.remove_etcd_node }
    - { role: kubernetes.kubespray.reset, tags: reset, when: reset_nodes|default(True)|bool }

# Currently cannot remove first master or etcd
- hosts: "{{ node | default('kube_control_plane[1:]:etcd[1:]') }}"
  gather_facts: no
  environment: "{{ proxy_disable_env }}"
  roles:
    - { role: kubernetes.kubespray.kubespray_defaults, when: reset_nodes|default(True)|bool }
    - { role: kubernetes.kubespray.remove_node.post_remove, tags: post_remove }
