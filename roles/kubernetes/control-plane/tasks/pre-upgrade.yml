---
- name: "Pre-upgrade | Delete master manifests if etcd secrets changed"
  file:
    path: "{{ kube_manifest_dir }}/{{ item }}.yaml"
    state: absent
  with_items:
    - ["kube-apiserver", "kube-controller-manager", "kube-scheduler"]
  when: etcd_secret_changed|default(false)
