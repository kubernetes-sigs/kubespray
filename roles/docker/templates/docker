# Deployed by Ansible https://github.com/ansibl8s/setup-kubernetes
{% if kube_network_plugin == "flannel" %}
DAEMON_OPTS="{{ docker_config.daemon_opts.rstrip('\n') }} --bip=${FLANNEL_SUBNET} --mtu=${FLANNEL_MTU}"
{% elif docker_config.daemon_opts %}
DAEMON_OPTS="{{ docker_config.daemon_opts }}
{% endif %}
