apiVersion: v1
kind: Service
metadata:
  name: metrics-server
  namespace: kube-system
  labels:
    addonmanager.kubernetes.io/mode: Reconcile
    app.kubernetes.io/name: "metrics-server"
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: metrics-server
  ports:
  - name: https
    port: 443
    protocol: TCP
    targetPort: https
