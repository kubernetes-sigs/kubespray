pipeline {
  agent any
  stages {
    stage('') {
      steps {
        echo 'Start'
      }
    }
    stage('deploy') {
      steps {
        validateDeclarativePipeline 'Jenkinsfile'
      }
    }
  }
}