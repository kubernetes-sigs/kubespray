---
- name: reset | delete some files and directories
  file:
    path: "{{ item }}"
    state: absent
  with_items:
    - "{{ etcd_data_dir }}"
    - "{{ etcd_events_data_dir }}"
  ignore_errors: yes
  tags:
    - files
