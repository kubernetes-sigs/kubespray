ansible==11.13.0
# Needed for community.crypto module
cryptography==46.0.7
# Needed for jinja2 json_query templating
jmespath==1.1.0
# Needed for ansible.utils.ipaddr
netaddr==1.3.0
# Needed for ansible-vault
passlib==1.7.4
bcrypt==4.0.1
ara[server]==1.7.5
