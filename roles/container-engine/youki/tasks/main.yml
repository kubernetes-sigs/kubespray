---
- name: Youki | Install youki binary
  copy:
    src: "{{ local_release_dir }}/youki"
    dest: "{{ youki_bin_dir }}/youki"
    mode: "0755"
    remote_src: "{{ download_delegate == inventory_hostname }}"
