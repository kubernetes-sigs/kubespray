{% for cpolicy in cilium_bgp_peering_policies %}
---
apiVersion: "cilium.io/v2alpha1"
kind: CiliumBGPPeeringPolicy
metadata:
  name: "{{ cpolicy.name }}"
spec:
  {{ cpolicy.spec | to_yaml | indent(2) }}
{% endfor %}
