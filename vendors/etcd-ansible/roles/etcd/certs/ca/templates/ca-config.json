{
    "signing": {
        "default": {
            "expiry": "{{cert_expiry}}"
        },
        "profiles": {
            "server": {
                "expiry": "{{cert_expiry}}",
                "usages": [
                    "signing",
                    "key encipherment",
                    "server auth"
                ]
            },
            "client": {
                "expiry": "{{cert_expiry}}",
                "usages": [
                    "signing",
                    "key encipherment",
                    "client auth"
                ]
            },
            "peer": {
                "expiry": "{{cert_expiry}}",
                "usages": [
                    "signing",
                    "key encipherment",
                    "server auth",
                    "client auth"
                ]
            }
        }
    }
}
