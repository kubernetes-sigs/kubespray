ansible==12.3.0
# passlib 1.7.4 does not work with bcrypt 5
bcrypt<5
# Needed for community.crypto module
cryptography==50.0.1
# Needed for jinja2 json_query templating
jmespath==1.1.0
# Needed for ansible.utils.ipaddr
netaddr==1.3.0
# Needed for encrypt hash
passlib==1.7.4
