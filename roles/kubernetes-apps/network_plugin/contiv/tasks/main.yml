---

- name: Contiv | Create Kubernetes resources
  k8s:
    src: "{{ contiv_config_dir }}/{{ item.item.file }}"
    state: present
  with_items: "{{ contiv_manifests_results.results }}"
  run_once: true

- import_tasks: configure.yml
