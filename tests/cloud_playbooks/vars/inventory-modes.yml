inventories:
  ha:
    all:
      hosts:
        k8s-{{test_name}}-1:
          etcd_member_name: etcd-1
        k8s-{{test_name}}-2:
          etcd_member_name: etcd-2
        k8s-{{test_name}}-3:
          etcd_member_name: etcd-3
      children:
        kube-master:
          hosts:
            k8s-{{test_name}}-1:
            k8s-{{test_name}}-2:
        kube-node:
          hosts:
            k8s-{{test_name}}-3:
        etcd:
          hosts:
            k8s-{{test_name}}-1:
        vault:
          hosts:
            k8s-{{test_name}}-1:
            k8s-{{test_name}}-2:
            k8s-{{test_name}}-3:
        k8s-cluster:
          children:
            kube-master:
            kube-nodes:

  separate:
    all:
      hosts:
        k8s-{{test_name}}-1:
        k8s-{{test_name}}-2:
        k8s-{{test_name}}-3:
          etcd_member_name: etcd-3
      children:
        kube-master:
          hosts:
            k8s-{{test_name}}-1:
        kube-node:
          hosts:
            k8s-{{test_name}}-2:
        etcd:
          hosts:
            k8s-{{test_name}}-1:
        vault:
          hosts:
            k8s-{{test_name}}-3:
        k8s-cluster:
          children:
            kube-master:
            kube-nodes:

  aio:
    all:
      hosts:
        k8s-{{test_name}}-1:
          etcd_member_name: etcd-1
      children:
        kube-master:
          hosts:
            k8s-{{test_name}}-1:
        kube-node:
          hosts:
            k8s-{{test_name}}-1:
        etcd:
          hosts:
            k8s-{{test_name}}-1:
        vault:
          hosts:
            k8s-{{test_name}}-1:
        k8s-cluster:
          children:
            kube-master:
            kube-nodes:

  default:
    all:
      hosts:
        k8s-{{test_name}}-1:
          etcd_member_name: etcd-1
        k8s-{{test_name}}-2:
        k8s-{{test_name}}-3:
      children:
        kube-master:
          hosts:
            k8s-{{test_name}}-1:
        kube-node:
          hosts:
            k8s-{{test_name}}-2:
        etcd:
          hosts:
            k8s-{{test_name}}-1:
        vault:
          hosts:
            k8s-{{test_name}}-1:
        k8s-cluster:
          children:
            kube-master:
            kube-nodes:
