- hosts: all
  tasks:
    - name: "run yum update"
      yum:
        update_cache: true

    - name: "install python3"
      yum:
        name: python3
        update_cache: true
        state: present

