{% for cpool in cilium_loadbalancer_ip_pools %}
---
apiVersion: "cilium.io/v2alpha1"
kind: CiliumLoadBalancerIPPool
metadata:
  name: "{{ cpool.name }}"
spec:
  blocks:
{% for cblock in cpool.cidrs %}
  - cidr: "{{ cblock }}"
{% endfor %}
{% endfor %}
