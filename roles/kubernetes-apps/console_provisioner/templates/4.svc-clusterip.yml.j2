apiVersion: v1
kind: Service
metadata:
  name: console-clusterip
  namespace: console-system
spec:
  type: ClusterIP
  ports:
    - name: "https"
      port: 8443
      targetPort: 8443
    - name: "http"
      port: 8000
      targetPort: 8000
  selector:
    app: console
    hypercloud: ui
