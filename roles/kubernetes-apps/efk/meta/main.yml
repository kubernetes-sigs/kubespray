---
dependencies:
  - role: kubernetes-apps/efk/elasticsearch
  - { role: 'kubernetes-apps/efk/fluentd', when: efk_log_collector == 'fluentd' }
  - { role: 'kubernetes-apps/efk/fluent-bit', when: efk_log_collector == 'fluent-bit' }
  - role: kubernetes-apps/efk/kibana
