---
dependencies:
  - role: download
    file: "{{ downloads.calico_cni }}"
    tags: download
  - role: download
    file: "{{ downloads.calico_node }}"
    tags: download
  - role: download
    file: "{{ downloads.calicoctl }}"
    tags: download
  - role: download
    file: "{{ downloads.hyperkube }}"
    tags: download
