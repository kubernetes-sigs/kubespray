---
- name: Install NFS common utilities
  hosts: kube_node
  become: yes
  tasks:
    - name: Ensure nfs-common is installed
      ansible.builtin.package:
        name: nfs-common
        state: present
