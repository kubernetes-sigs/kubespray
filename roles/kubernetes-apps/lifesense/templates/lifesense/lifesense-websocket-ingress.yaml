apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{item}}
  namespace: {{lifesense_env}}  
spec:
  rules:
  - host: {{item}}
    http:
      paths: 
      - path: /websocket
        backend:
          serviceName: websocket-svc
          servicePort: 8080                      
