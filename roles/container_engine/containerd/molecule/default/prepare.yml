---
- name: Prepare
  hosts: all
  gather_facts: False
  become: true
  vars:
    ignore_assert_errors: true
  roles:
    - role: kubespray_defaults
    - role: bootstrap_os
    - { role: kubernetes/preinstall, tags: ["bootstrap_os"] }
