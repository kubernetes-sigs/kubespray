---
# Kubespray constants

kube_proxy_deployed: "{{ 'addon/kube-proxy' not in kubeadm_init_phases_skip }}"

# The oldest version supported to install or upgrade from (same as calico_version in the previous branch)
calico_min_version_required: "v3.15.2"

# The oldest version supperted to install or upgrade from (same as kube_version in the previous branch)
kube_version_min_required: "v1.18.9"
