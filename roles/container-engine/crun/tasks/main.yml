---
- name: Install crun binary
  copy:
    src: "{{ install_source_dir }}/{{ downloads.crun.dest }}"
    dest: "{{ bin_dir }}/crun"
    mode: "0755"
    remote_src: "{{ download_delegate == inventory_hostname }}"
