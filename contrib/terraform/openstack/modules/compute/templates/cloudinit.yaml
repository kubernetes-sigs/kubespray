#cloud-config  # yamllint disable-line rule:require-starting-space
## in some cases novnc console access is required
## it requires ssh password to be set
#ssh_pwauth: yes  # yamllint disable-line rule:require-starting-space
#chpasswd:  # yamllint disable-line rule:require-starting-space
#  list: |
#    root:secret
#  expire: False

## in some cases direct root ssh access via ssh key is required
#disable_root: false  # yamllint disable-line rule:require-starting-space

## in some cases additional CA certs are required
#ca-certs:  # yamllint disable-line rule:require-starting-space
#  trusted: |
#      -----BEGIN CERTIFICATE-----
