- name: Set up kubeconfig for calicoctl using kubeadm control plane
  include_tasks: install_kdd_certs_control_plane.yml
  when: kubeadm_control_plane

- name: Set up kubeconfig for calicoctl using first node and sync
  include_tasks: install_kdd_certs_normal.yml
  when: not kubeadm_control_plane
