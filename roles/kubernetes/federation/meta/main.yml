---
dependencies:
  - role: download
    file: "{{ downloads.hyperkube }}"
    tags: [download, hyperkube]
  - role: download
    file: "{{ downloads.kubernetes_client }}"
    tags: [download, hyperkube]
