apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{item}}
  namespace: {{lifesense_env}}
  annotations:
    ingress.kubernetes.io/app-root: {{item}}
spec:
  rules:
  - host: {{item}}
    http:
      paths: 