---
# The calico version installed or upgraded to
calico_version_default: "v3.15.1"

# Required minimum version of calico to upgrade from
# (should be the same as calico_version from previous branch)
calico_version_min_required: "v3.7.3"
