---
dependencies:
  - role: download
    file: "{{ downloads.heapster_grafana }}"
  - role: download
    file: "{{ downloads.heapster }}"
  - role: download
    file: "{{ downloads.heapster_influxdb }}"
