---
apiVersion: "{{ 'storage.k8s.io/v1' if kube_version is version('v1.18.0', '>=') else 'storage.k8s.io/v1beta1' }}"
kind: CSIDriver
metadata:
  name: ebs.csi.aws.com
spec:
  attachRequired: true
  podInfoOnMount: false
